/* app.tcss — Layout terminal UEXInfo */

Screen {
    background: $surface;
    layout: vertical;
    layers: default overlay;
}

/* ── OutputView — sortie principale ─────────────────────────────────────── */

OutputView {
    height: 1fr;
    padding: 0 1;
    background: $surface;
    scrollbar-gutter: stable;
}

/* ── CompletionList — liste déroulante Ctrl+↓ ───────────────────────────── */

CompletionList {
    display: none;
    height: auto;
    max-height: 12;
    background: $panel;
    border: tall $accent;
    margin: 0 1;
}

CompletionList ListItem {
    padding: 0 1;
}

CompletionList ListItem.--highlight {
    background: $accent;
}

/* ── PromptWidget — ligne de saisie ─────────────────────────────────────── */

PromptWidget {
    height: auto;
    background: $panel;
    border-top: solid $accent;
}

#prompt-row {
    height: auto;
    padding: 0 1;
    align: left middle;
}

#prompt-symbol {
    color: cyan;
    width: auto;
    padding: 0 1 0 0;
    text-style: bold;
}

#prompt-input {
    width: 1fr;
    border: none;
    background: $surface-lighten-1;
    color: $text;
    padding: 0 1;
}

#prompt-input:focus {
    border: none;
    background: $surface-lighten-2;
}

/* ── StatusBar — état courant (1 ligne) — styles gérés en DEFAULT_CSS ───── */

StatusBar {
    height: 1;
}

/* ── Footer ─────────────────────────────────────────────────────────────── */

Footer {
    background: $accent-darken-2;
}
